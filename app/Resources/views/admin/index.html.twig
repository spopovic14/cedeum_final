{% extends 'base.html.twig' %}

{% block body %}

    <div class="row">
        <div class="box">
            <div class="col-lg-12">

                <ul>
                    {% for page in pages %}

                        <li><a href="admin/pages/{{ page.id }}">{{ page.title }}</a></li>

                    {% endfor %}
                </ul>

            </div>
        </div>
    </div>

{% endblock %}

