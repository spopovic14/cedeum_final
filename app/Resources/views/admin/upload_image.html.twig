{% extends 'base.html.twig' %}

{% block body %}

    <div class="row">
        <div class="box">
            <div class="col-lg-12">

                {{ form_start(form) }}

                    {{ form_row(form.name) }}
                    {{ form_row(form.picture) }}

                    <button type="submit" class="btn btn-primary">Sacuvaj</button>

                {{ form_end(form) }}

            </div>
        </div>
    </div>

{% endblock %}
