{% extends 'base.html.twig' %}

{% block body %}

    <!-- <div class="row">
        <div class="box">
            <div class="col-xl-12">
                <h1 class="text-center">{{ article|localize('title') }}</h1>
                <img src="{{ asset('uploads/article_pictures/' ~ article.Picture) }}" alt="Nema" class="img-full img-responsive">
                {{ article|localize('content')|raw }}
                </br>
                <span class="text-muted">{{ article.releaseDate|date("d/m/Y") }}</span>
            </div>
        </div>
    </div> -->

    <div class="row">
        <div class="col-xs-12">
            <h1 class="text-center">{{ article|localize('title') }}</h1>
        </div>
    </div>

    <div class="row pt-18 pb-32">
        <div class="col-xs-12 col-md-8 col-centered center-block">
            <img src="{{ asset('uploads/article_pictures/' ~ article.picture) }}" class="img-full img-responsive">
        </div>
    </div>

    <div class="row">
        <div class="col-xs-12">
            {{ article|localize('content')|raw }}

            <br/>
            <p><span class="text-muted">{{ article.releaseDate|date("d/m/Y") }}</span></p>
        </div>
    </div>

{% endblock %}
