{% extends 'base.html.twig' %}

{% block content %}

    {% if page.raw == null %}

        <div class="row">
            <div class="box">
                <div class="col-lg-12">
                    {{ page|localizePage('content')|raw }}
                </div>
            </div>
        </div>

    {% endif %}

{% endblock %}

{% block body %}

    {% if page.raw %}

        {{ page|localizePage('content')|raw }}

    {% endif %}

{% endblock %}

{% block title %}
    {{ page|localizePage('title') }}
{% endblock %}
